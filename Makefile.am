#
#
# Makefile.am for ldap-utils c programs
#
#
RM = rm -f
ECHO = echo
DIR = src
if HAVE_OLD
  OLD_DIR = old
endif
SUBDIRS = lib src man $(OLD_DIR)
DIST_SUBDIRS = lib src man old
EXTRA_DIST = man
ACLOCAL_AMFLAGS = -I m4
